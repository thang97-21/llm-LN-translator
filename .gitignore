# ── Secrets ─────────────────────────────────────────────────────────────────
.env
*.env.local
*.env.*.local

# ── Development Files ─────────────────────────────────────────────────────
dev/
_design/
_docs/
*.tmp.md
tests/

# ── Agent/Skill Authoring (local only — never pushed) ──────────────────────
# Vendored third-party API docs, agent/skill definitions, and QC calibration
# samples that include full-text commercial ebook extracts. None of this is
# the tool's own source code.
.agents/
.codex/
.grok/
dsh/

# ── Foreign nested repositories (never vendor another project's history) ───
epub-to-xtc-converter/

# ── Claude Local Settings ───────────────────────────────────────────────────
.claude/
CLAUDE.local.md
.claude.json
CLAUDE.md

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.eggs/
venv/
.venv/
env/
.python-version
*.log

# ── Node / TypeScript ───────────────────────────────────────────────────────
node_modules/
*.tsbuildinfo
dist-ts/

# ── IDE ─────────────────────────────────────────────────────────────────────
.vscode/settings.json
.vscode/*.code-workspace
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Workspace Artifacts (don't commit actual translation data) ──────────────
raw/*
!raw/.gitkeep
output/*
!output/.gitkeep
work/*
!work/.gitkeep
logs/*
!logs/.gitkeep
cache/*
!cache/.gitkeep

# ── MCP Server State ────────────────────────────────────────────────────────
.mcp_state/

# ── Copyrighted Workspaces ────────────────────────────────────────────────────────
raw/
work/

# ── IDE-Workspace / Agent Authoring (local only — never pushed) ────────────
# Only src/ (the actual DeepSeek MTLS source) and src/prompt/ (the shipped
# translation prompts) go to the remote. Everything that configures how an
# AI agent develops or operates this repo stays local: subagent/skill
# definitions, the orchestrator persona, and dev planning notes.
.github/
/AGENTS.md
/PLANNING.md
bible/
bibles/