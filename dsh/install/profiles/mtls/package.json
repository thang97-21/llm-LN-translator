{
  "name": "mtls-profile",
  "version": "0.1.0",
  "private": true,
  "description": "MTLS pipeline desk profile for DeepSeek Harness — boots the mtls agent preset with the merged Mirei+MTLS persona and the dsh-client-mtls pipeline workspace plugin.",
  "dependencies": {
    "dsh-client-mtls": "link:D:/MTLS/dsh/client-plugins/dsh-client-mtls"
  },
  "dsh": {
    "profile": {
      "bundles": [
        "@deepseek-ai/dsh-base",
        "@deepseek-ai/dsh-web-app",
        "dsh-client-mtls"
      ]
    }
  }
}
