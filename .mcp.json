{
  "mcpServers": {
    "llm-translator": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "src.Deepseek.mcp.server"],
      "env": {
        "DEEPSEEK_API_KEY": "${DEEPSEEK_API_KEY}"
      }
    }
  }
}
