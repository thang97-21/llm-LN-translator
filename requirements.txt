# LLM Translator — Minimal Dependencies
# Only what Librarian + Builder + the selected Phase 2 provider need.

# Phase 2 provider SDKs
# anthropic floor raised for src/Anthropic's real Messages API route: adaptive
# thinking (thinking.type="adaptive") and the Message Batches API used by
# src/Anthropic/batch.py both postdate 0.39.0, which only ever needed to
# support DeepSeek/Qwen's Anthropic-*compatible* endpoints, not these
# newer-generation Claude-5 features. 0.125.0 is current as of 2026-08-20.
anthropic>=0.100.0
openai>=2.0.0

# YAML configuration
pyyaml>=6.0

# Environment variables (.env files)
python-dotenv>=1.0

# EPUB parsing (Librarian)
lxml>=5.0
beautifulsoup4>=4.12

# Image processing (Builder)
Pillow>=10.0

# Token counting for cache/pricing analytics (src/common/token_telemetry.py).
# Local only, no auth — tried HuggingFace's AutoTokenizer for DeepSeek's own
# repos and reverted: needs HF_TOKEN in practice (rate-limited/gated), which
# isn't a trade worth making for a token-counting utility. tiktoken o200k_base
# is the approximation, cl100k_base a secondary fallback.
tiktoken>=0.7

# MCP server (Phase 6) — stdio tool server for Claude Desktop / Claude Code.
# Requires the 2.x server API (mcp.server.mcpserver.MCPServer); FastMCP was
# removed in 2.0.0, so 1.x will NOT work. Floor bumped to 2.0 accordingly.
mcp>=2.0.0

# Optional: smart typographic quotes in Builder output
# smartypants>=2.0
